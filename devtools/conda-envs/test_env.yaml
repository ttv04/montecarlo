name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - numpy
  - networkx
  

    # Pip-only installs
  #- pip:
  #  - codecov

